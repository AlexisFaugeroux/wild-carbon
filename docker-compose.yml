services:
  backend:
    build: ./back
    env_file:
      - ./.env
    depends_on:
      - db
    networks:
      - networksData

  frontend:
    build: ./front
    volumes:
      - ./front/src:/app/src
    networks:
      - networksData

  db:
    image: postgres
    restart: always
    volumes:
      - postgresData:/var/lib/postgresql/data
    env_file:
      - ./.env
    networks:
      - networksData

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
    networks:
      - networksData

volumes:
  postgresData:

networks:
  networksData: